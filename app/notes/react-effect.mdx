export const title = 'React effects'

If React depends on DOM state that was changed in a useEffect, it should be useLayoutEffect instead. If the change is strictly a one way update, pick useEffect instead
