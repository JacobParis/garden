export const title = 'Publishing an npm module'

You must be logged in to your npm account. Run the command `npm whoami` to check, and if you're not logged in, do so with `npm adduser`

To publish a package, run `npm publish --access public`
